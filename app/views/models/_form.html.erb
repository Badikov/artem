<%= form_for model, remote: true, :html => { :class => 'form-horizontal model' } do |f| %>
  <div class="field">
    
    <%= f.text_field :name %>
  </div>
  <div class="field">
    
    <%= f.text_field :name2  %>
  </div>
  <div class="actions">
    <%= f.submit('Сохранить') %>
  </div>
<% end %>