<%= form_for(@black_list) do |f| %>
  <% if @black_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@black_list.errors.count, "error") %> prohibited this black_list from being saved:</h2>

      <ul>
      <% @black_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Номер телефона' %><br>
    <%= f.text_field :phone %>
  </div>
  <div class="field">
    <%= f.label 'Коментарий' %><br>
    <%= f.text_area(:coment, size: '50x10')  %>
  </div>
  <div class="actions">
    <%= f.submit('Сохранить') %>
  </div>
<% end %>
